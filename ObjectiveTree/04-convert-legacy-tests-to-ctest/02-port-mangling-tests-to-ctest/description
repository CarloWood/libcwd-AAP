Port the legacy mangling-focused manual tests tests/mangling.cc and tests/mangling2.cc into deterministic CTest-backed tests under testsuite/ctest. Preserve the libcwd API behavior being exercised: demangling and type_info_of output for the representative C++ types/templates, but update the tests to assert expected demangled strings directly rather than depending on manual stdout inspection.
