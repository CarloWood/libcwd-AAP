Report inline source scopes in location_ct

Introduce a location lookup result that distinguishes the enclosing physical
symbol from the effective source-level function. Use DWARF inline-scope metadata
to report DW_TAG_inlined_subroutine bodies with the inlined function name, and
add CTest coverage documenting both real inline-body handling and
default-argument call-site metadata limitations.
