Investigate the remaining legacy manual files tests/initbug.cc, tests/initbug_GlobalObject.cc, and tests/syslog.cc before deciding whether to port, replace, or retire them. Determine the libcwd behavior each file was meant to exercise, whether that behavior is still relevant after the malloc-debugging removal and CMake migration, and propose concrete follow-up CTest goals only for behavior that should remain covered.
