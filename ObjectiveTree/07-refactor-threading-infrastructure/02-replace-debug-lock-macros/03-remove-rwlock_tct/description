Remove the remaining libcwd::_private_::rwlock_tct infrastructure after the debug object/channel registries have moved to threadsafe access types. Replace the remaining threadlist_instance uses in threading_tsd_init and the fatal-output thread-cancellation path with a typed RAII-protected thread list, remove obsolete object_files_instance/debug_objects_instance/debug_channels_instance rwlock initialization and mutex-instance entries when no longer used, and preserve cancellation-safety and debug/deadlock diagnostics or replace them with equivalent checks.
