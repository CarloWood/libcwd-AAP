Produce `$PLANROOT/current_objective/processing-order.txt`, generated with help from `findsymbol` where useful, listing all classes and structs in the order they should be edited: first test `.cc` definitions, then test `.h` definitions while confirming no other test file kinds define types, then non-test `.cc` definitions, then non-public `.h`/`.inl` definitions outside `include/libcwd`, and finally the remaining public header definitions.
