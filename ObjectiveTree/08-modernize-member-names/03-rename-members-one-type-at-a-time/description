Rename member variables one class or struct at a time, following `02*/processing-order.txt` and the approved map in `01*/member_name_overview.md`: update the current type declaration, apply targeted replacements in likely affected `.cc` files only, do not automatically search-and-replace headers, then make the project pass `aap-build` and `aap-test` before proceeding to the next type.
