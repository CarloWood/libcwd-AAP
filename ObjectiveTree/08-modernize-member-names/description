Ensure member variable naming is consistent across the codebase: private and protected class members use snake_case with a trailing underscore and no legacy member prefixes such as M_ or m_, while public struct members use snake_case without a trailing underscore or legacy member prefixes. Update references, comments, and current documentation accordingly while preserving historical changelog text, and verify each refactoring batch with build and tests.
