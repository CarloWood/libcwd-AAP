Migrate the tutorial into the shared Doxygen Awesome documentation shell without losing its six-part learning sequence, setup flow, generated examples, or captured output behavior. Tutorial content may remain authored through its existing generation pipeline only where necessary, but the delivered pages must share the final sidebar/navigation/theme/search conventions, cross-link consistently to the reference manual, and avoid parallel legacy-only layout code that would need to be discarded later.
